#Cyber Risk Quantifier
A FAIR-based cyber risk quantification tool that uses Monte Carlo simulation to convert qualitatative security assessments into dollar-denominated loss distributions.
## How to run
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
##Documentation
See the `docs/` folder for full project documentation:
-One-Pager
-PRD
User Stories & Use Cases
-Technical Design Document
-Architecture
-Threat Model
